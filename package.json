{
  "name": "procent-vanzari-mvp",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply procent_vanzari_db --local",
    "db:migrate:remote": "wrangler d1 migrations apply procent_vanzari_db --remote"
  },
  "devDependencies": {
    "wrangler": "^4.71.0"
  }
}
